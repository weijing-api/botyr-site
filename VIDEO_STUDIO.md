# 照片成片工作台 Beta

## 当前能力

- 读取用户当前选中的 AI 选题、标题和口播。
- 上传 1–6 张产品、门店或案例照片。
- 自动识别首张商品图，并按“3 秒钩子、真实画面证据、顾客价值、行动引导”生成可编辑解说。
- 商品解说会经过事实审校，未确认的价格、功效、口感、销量和优惠不会写入成片。
- 可选上传用户自己的口播或有授权的音乐。
- 默认通过 Cloudflare Workers AI MeloTTS 自动生成普通话讲解，无需额外密钥。
- 配置腾讯云语音密钥后会优先使用腾讯云，失败时自动切回 Cloudflare。
- 可自动生成低音量氛围音乐，并在有讲解时降低音乐音量。
- 自动生成 9:16 竖屏字幕、轻微推拉和画面切换。
- 完全在浏览器本地合成，素材不会上传服务器。
- 输出可预览、可下载的 WebM 视频。

## 浏览器要求

建议使用最新版 Microsoft Edge 或 Google Chrome。浏览器需要支持：

- `Canvas.captureStream`
- `MediaRecorder`
- `createImageBitmap`
- WebM 编码

## 自动讲解配置

默认语音使用已有的 Workers AI 绑定，不需要再配置密钥。

如需启用腾讯云音色，可以选择配置以下两个加密变量：

- `TENCENT_SECRET_ID`
- `TENCENT_SECRET_KEY`

密钥必须通过 Cloudflare Worker 的“变量和机密”页面或 Wrangler Secret 添加，不能写进 `index.html`、前端 JavaScript 或 Git 仓库。

Worker 提供 `POST /tts`，只允许站点白名单来源调用，单次最多合成 150 个中文字符。腾讯云未配置或调用失败时，会自动使用 Cloudflare 中文语音。

Worker 还提供 `POST /generate-narration`，接收商家确认的商品名称、行业和真实卖点，生成并审校适合短视频配音的商品解说。前端允许商家在生成语音前直接修改或按卖点重写。

腾讯云侧需要先开通“语音合成”服务，并确保密钥具有调用语音合成 API 的权限。

## 自动配乐

第一版音乐由浏览器 Web Audio 实时生成，不使用网络歌曲，因此不会把未经授权的音乐打包进网站。音乐会以低音量混入 WebM；存在讲解时会进一步降低背景音乐音量。

## 为什么当前不是 MP4

静态 GitHub Pages 无法稳定执行 FFmpeg 服务端渲染。浏览器对 MP4 编码支持也不统一，因此第一版使用 WebM 验证“素材上传 → 自动编排 → 视频下载”的完整链路。

下一阶段建议由 Cloudflare Worker 接收渲染任务，再交给独立 FFmpeg 服务完成：

1. 素材上传到对象存储。
2. Worker 创建任务并返回任务 ID。
3. FFmpeg 服务生成 H.264 MP4。
4. 前端轮询任务状态并提供下载。
5. 成片在设定时间后自动删除，控制存储成本。

## 暂未包含

- 上传视频片段后的智能剪辑。
- H.264 MP4 云端导出。
- 数字人和口型同步。
- 自动发布到短视频平台。
